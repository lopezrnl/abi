{
  "buildCommand": "cd galleryniabby && npm install && npm run build",
  "outputDirectory": "galleryniabby/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}